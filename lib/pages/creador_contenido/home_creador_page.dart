import 'package:flutter/material.dart';

class HomeCreadorPage extends StatelessWidget {
  const HomeCreadorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}